# 🚨 СРОЧНАЯ ИНСТРУКЦИЯ - Что делать ПРЯМО СЕЙЧАС

## Ваша ситуация:

- ✅ Вы в WSL
- ❌ В WSL нет интернета
- ❌ Вы пытались запустить `prepare_offline.sh` в WSL
- ⚠️ **Это не сработает!** Скрипт нужен интернет для скачивания Docker образов

## ✅ Правильный порядок действий:

### Шаг 1: Закрыть WSL, открыть Windows PowerShell

```powershell
# В Windows (не в WSL!), нажмите Win+X -> "Windows PowerShell"
# Или откройте "PowerShell" из меню Пуск
```

### Шаг 2: В Windows PowerShell - скачать Docker образы

```powershell
# Перейти в папку проекта
cd C:\Users\Emin\Desktop\SMU

# Скачать Docker образы (требует интернет в Windows!)
docker pull python:3.11
docker pull postgres:15
docker pull redis:7
docker pull elasticsearch:7.17.13

# Сохранить их в один файл
docker save python:3.11 postgres:15 redis:7 elasticsearch:7.17.13 -o docker-images-smu.tar

# Это создаст файл docker-images-smu.tar (~2.5 GB)
```

**Проверка**: Убедитесь что файл создан:

```powershell
ls docker-images-smu.tar
# Должен показать файл размером ~2.5 GB
```

### Шаг 3: Теперь можно вернуться в WSL

```bash
# В WSL
cd /mnt/c/Users/Emin/Desktop/SMU

# Загрузить Docker образы из файла (интернет НЕ нужен!)
docker load -i docker-images-smu.tar
```

Это займёт 2-3 минуты. Вы увидите:

```
Loaded image: python:3.11
Loaded image: postgres:15
Loaded image: redis:7
Loaded image: elasticsearch:7.17.13
```

### Шаг 4: Проверить что образы загружены

```bash
docker images
```

Должны увидеть все 4 образа.

### Шаг 5: Теперь можно собрать проект

```bash
cd /mnt/c/Users/Emin/Desktop/SMU
docker-compose build
```

Это будет работать БЕЗ интернета! Docker использует уже загруженные образы.

### Шаг 6: Запустить проект

```bash
docker-compose up -d
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py createsuperuser
```

## 🎉 Готово!

Проверить:

```bash
curl http://localhost:8000/api/v1/
```

---

## ❓ FAQ

**Q: У меня нет интернета даже в Windows, что делать?**  
A: Скачайте образы на другом компьютере с интернетом, сохраните `docker-images-smu.tar` на флешку, перенесите на свой компьютер.

**Q: Можно ли использовать `.tar.gz` вместо `.tar`?**  
A: Да, но на Windows проще использовать `.tar`. Если хотите сжать:

```powershell
docker save python:3.11 postgres:15 redis:7 elasticsearch:7.17.13 | gzip > docker-images-smu.tar.gz
```

**Q: Сколько времени займёт скачивание образов?**  
A: 5-10 минут в зависимости от скорости интернета.

**Q: Можно ли удалить docker-images-smu.tar после загрузки?**  
A: Да, после `docker load` файл можно удалить или оставить для повторного использования.

---

**Ключевой момент**: 🔑

- **Windows PowerShell** (С интернетом) → скачать образы → сохранить в файл
- **WSL** (БЕЗ интернета) → загрузить из файла → собрать проект

**Текущий файл**: C:\Users\Emin\Desktop\SMU\docker-images-smu.tar  
**Размер**: ~2.5 GB  
**Нужен интернет**: Только для создания файла в Windows
