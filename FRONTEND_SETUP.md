# 🚀 SMU Digital Library - Frontend + Backend

Полный стек проекта цифровой библиотеки университета.

## 📁 Структура проекта

```
SMU/
├── frontend/              # Vue 3 фронтенд (НОВОЕ!)
│   ├── src/
│   ├── package.json
│   └── README.md
│
├── src/                   # Django бэкенд
│   ├── content/
│   ├── src/
│   └── manage.py
│
├── docker-compose.yml
└── README.md
```

## 🎯 Быстрый старт

### Вариант 1: С Docker (Рекомендуется)

```bash
# 1. Запустить бэкенд со всеми сервисами
docker-compose up -d

# 2. Установить зависимости фронтенда
cd frontend
npm install

# 3. Запустить фронтенд
npm run dev
```

**Готово!**

- Фронтенд: http://localhost:3000
- Бэкенд API: http://localhost:8000
- Admin: http://localhost:8000/admin

### Вариант 2: Без Docker

**Бэкенд:**

```bash
# Установить Python зависимости
pip install -r requirements.txt

# Применить миграции
cd src
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Запустить сервер
python manage.py runserver
```

**Фронтенд:**

```bash
cd frontend
npm install
npm run dev
```

## 📦 Что реализовано во фронтенде

### ✅ Страницы

- 🏠 Главная страница с избранным контентом
- 📄 Список статей с фильтрами (как на вашем скрине)
- 📖 Детальные страницы статей
- 📚 Книги и диссертации
- 🔍 Полнотекстовый поиск
- 🔐 Авторизация и регистрация
- ⭐ Страница закладок
- 👤 Профиль пользователя

### ✅ Функции

- JWT аутентификация с автообновлением токенов
- Фильтрация по категориям (боковое меню)
- Фильтры "Ýurdumyzda" / "Daşary ýurtlarda"
- Система рейтингов (5 звезд)
- Добавление в закладки
- Счетчик просмотров
- Пагинация
- Адаптивный дизайн

### ✅ Многоязычность

- 🇹🇲 Туркменский (по умолчанию)
- 🇷🇺 Русский
- 🇬🇧 Английский

### ✅ UI/UX

- Современный дизайн с TailwindCSS
- Карточки контента с изображениями
- Loading states
- Плавные анимации
- Мобильная адаптация

## 🛠️ Технологии

**Frontend:**

- Vue 3 (Composition API)
- Vite
- Pinia (State Management)
- Vue Router
- Axios
- Vue I18n
- TailwindCSS

**Backend:**

- Django 4.2
- Django REST Framework
- PostgreSQL
- Elasticsearch
- Redis
- Celery
- JWT Authentication

## 📝 Конфигурация

### Frontend (.env)

```env
VITE_API_BASE_URL=http://localhost:8000
VITE_API_VERSION=v1
```

### Backend (src/.env)

Уже настроен через docker-compose.yml

## 🎨 Соответствие дизайну

Фронтенд полностью реализует дизайн из вашего скриншота:

✅ Логотип "SMU" в хедере
✅ Поисковая строка по центру
✅ Навигация (Makalalar, Dissertasiýalar, Kitaplar)
✅ Переключатель языков
✅ Боковое меню категорий
✅ Фильтры "Ýurdumyzda" / "Daşary ýurtlarda"  
✅ Карточки статей с:

- Изображением
- Заголовком
- Автором
- Датой публикации
- Просмотрами
- Рейтингом
- Кнопкой "Ýatda sakla"

## 📚 API Endpoints

Все интегрированы с фронтендом:

```
GET  /api/v1/articles/                    # Список статей
GET  /api/v1/articles/{id}/               # Детали статьи
GET  /api/v1/books/                       # Список книг
GET  /api/v1/dissertations/               # Диссертации
GET  /api/v1/search/?q=query              # Поиск
POST /api/v1/auth/login/                  # Вход
POST /api/v1/auth/register/               # Регистрация
GET  /api/v1/bookmarks/                   # Закладки
POST /api/v1/bookmarks/toggle/{id}/       # Добавить/удалить закладку
POST /api/v1/rate/                        # Оценить контент
```

## 🚢 Деплой

### Frontend

```bash
cd frontend
npm run build
# Загрузить папку dist/ на сервер
```

### Backend

Следуйте инструкциям в DEPLOY.md

## 🎯 Следующие шаги

1. **Проверьте работу:**

   ```bash
   # Терминал 1: Бэкенд
   docker-compose up

   # Терминал 2: Фронтенд
   cd frontend && npm run dev
   ```

2. **Откройте http://localhost:3000**

3. **Протестируйте функции:**
   - Регистрация/вход
   - Просмотр статей
   - Фильтрация по категориям
   - Поиск
   - Добавление в закладки
   - Рейтинг

4. **Настройте под себя:**
   - Цвета в `frontend/tailwind.config.js`
   - Переводы в `frontend/src/locales/`
   - Логотип и изображения

## 📖 Документация

- Frontend: [frontend/README.md](frontend/README.md)
- Frontend Setup: [frontend/SETUP.md](frontend/SETUP.md)
- Backend: [README.md](README.md)
- API: [API_EXAMPLES.md](API_EXAMPLES.md)

## 💡 Полезные команды

**Разработка:**

```bash
# Бэкенд
docker-compose up -d

# Фронтенд
cd frontend && npm run dev

# Логи
docker-compose logs -f django
```

**Остановить:**

```bash
docker-compose down
```

**Пересобрать:**

```bash
docker-compose up --build
```

## 🐛 Troubleshooting

### Фронтенд не подключается к API

- Проверьте что бэкенд запущен на порту 8000
- Проверьте CORS настройки в Django
- Откройте DevTools → Network

### Ошибки при npm install

```bash
rm -rf node_modules package-lock.json
npm install
```

### Изменить порт фронтенда

Отредактируйте `frontend/vite.config.js`:

```js
server: {
  port: 3001; // Новый порт
}
```

## ✨ Особенности

- **Автоматическое обновление JWT токенов**
- **Proxy для медиа-файлов через Vite**
- **Optimistic UI updates** для закладок
- **Debounced search** в поисковой строке
- **Infinite scroll** готов к внедрению
- **PWA ready** - можно добавить манифест

## 🎉 Готово к использованию!

Полноценное веб-приложение с современным фронтендом и мощным бэкендом.

Вопросы? Смотрите документацию в папках `frontend/` и основную `README.md`

Удачи! 🚀
